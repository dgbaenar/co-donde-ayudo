# Message template

```text
<type>(<optional-scope>): <imperative summary>

<why the change was needed and what changed>

Tests:
- <command and observed result>

Risks:
- <verified risk or none>
```

Allowed common types include `feat`, `fix`, `docs`, `test`, `refactor`, `chore`, `build`, and `ci`.
